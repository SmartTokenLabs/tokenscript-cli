{
	"$schema": "../tstemplate.schema.json",
	"name": "Entry Token",
	"description": "An example of using TokenScript to interact with ioT devices",
	"templateFields": [
		{
			"name": "TokenScript name",
			"prompt": "Enter a name for the TokenScript project"
		},
		{
			"name": "Contract address",
			"prompt": "Enter the ethereum contract address for the TokenScript"
		},
		{
			"name": "TokenScript bridge URL",
			"prompt": "Enter the TokenScript proxy URL"
		}
	]
}
